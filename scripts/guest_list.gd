extends Control

@export var guest_scene: PackedScene

var guest_list: Array 

func _ready():
	#guest_list = SERVICE.get_guest_list()
	#var new_guest = guest_scene.instantiate()
	#new_guest.constructor(guest_list[0])
	#add_child(new_guest)
	pass
	
